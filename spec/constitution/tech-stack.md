# Tech stack

**Estado:** propuesta pendiente de aprobación para implementación

Recomendación por viabilidad: React + Vite + TypeScript como SPA. Cliente HTTP tipado; librería de server-state recomendada (p. ej. TanStack Query) para polling/cache; router SPA; testing con Vitest + Testing Library.

**PENDING:** aprobar framework/librerías exactas y package manager antes del bootstrap de `app/`. La SDD funcional no depende de esa elección.

No se requiere SSR para el alcance actual.
